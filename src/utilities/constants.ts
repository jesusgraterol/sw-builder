// the characters that can be used to generate cache names
export const CACHE_NAME_CHARACTERS = 'abcdefghijklmnopqrstuvwxyz0123456789';

// the number of characters that will comprise the cache names
export const CACHE_NAME_LENGTH: number = 10;

// separates an application-owned prefix from its random version suffix
export const CACHE_NAME_SEPARATOR = '--';

// the name of the file that is built and placed in the outDir
export const OUTPUT_NAME: string = 'sw.js';
